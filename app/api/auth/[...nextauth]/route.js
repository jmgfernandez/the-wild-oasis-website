// ... - catchall segment
// all URLs that start with /api/auth, and then slash whatever we want will be handled by this route.js.

export { GET, POST } from "@/app/_lib/auth";
// 👆 syntax for importing and exporting at the same time
